# self-evaluation-site

ローカルで自己評価サイトのソースコードを管理するためのリポジトリです。

使い方:

- リポジトリ初期化: `git init`
- 初回コミット: `git add . && git commit -m "Initial commit"`
- GitHubに公開するには `gh repo create --public --source=. --remote=origin --push` を使用します（gh CLIが必要）
